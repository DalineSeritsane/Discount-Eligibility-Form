//Discount Eligibility Program

checkEligibility(
    const inputField = number.getElementById('inputField');
    const inputValue = inputField.getAttribute('value');

    let age = 18;

    if (age >= 65) {
        console.log("Senior Discount");
    }
    else {
        console
    }
)